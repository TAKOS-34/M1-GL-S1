1. Lancer le registre, puis le clientpuis le serveur 
2. Si on execute directement la ligne 14 alors on récupère un registry existant donc ça peut ne pas marcher, alors que la ligne 15 créer directement le registre
```java TD1.Server```\
```java TD1.Client 1099```
3. Serveur :
Server ready
The server prints: Hello, world!

Client :
response: Hello, world!
4. Serveur
5. Contrat entre le client - serveur
6. Erreur de connexion (couche transport)
7. bind peut causer une erreur si plusieurs clé sont liés au même objet, alors que rebind écrase l’ancien
8. Le host = null (marche quand même)
9. #image("images/f1.png")